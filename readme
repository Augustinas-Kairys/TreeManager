# TreeManagerPlugin

TreeManagerPlugin – A lightweight Bukkit/Spigot plugin for WorldGuard regions that restricts tree farming per zone. Fixes WorldGuard limitations: allows players to regrow trees with bonemeal even when default flags prevent it. Supports PlaceholderAPI for custom messages.


## Features

- **Zone-based Permissions**: Define different permission levels for tree farming areas using WorldGuard regions
- **Block Restrictions**: Control what blocks players can break and place within farm zones
- **PlaceholderAPI Integration**: Support for dynamic placeholders in messages
- **Customizable Messages**: Configure messages for different permission scenarios
- **Easy Management**: Reload configuration without restarting the server

## Dependencies

- **Required**: [WorldGuard](https://dev.bukkit.org/projects/worldguard)
- **Optional**: [PlaceholderAPI](https://www.spigotmc.org/resources/placeholderapi.6245/)

## Installation

1. Download the plugin JAR file
2. Place it in your server's `plugins` folder
3. Restart your server or use a plugin manager to load it
4. Configure `config.yml` and `messages.yml` in the `plugins/TreeManagerPlugin` folder
5. Use `/treemanager reload` to apply changes

## Configuration

### config.yml
Define your tree farm zones:


